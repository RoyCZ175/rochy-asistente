groq
edge-tts
pygame
SpeechRecognition
PyAudio
pyautogui
keyboard
pycaw
comtypes
# Activar/desactivar Wi-Fi y Bluetooth sin permisos de administrador (misma
# API que usa el propio Centro de actividades de Windows).
winrt-Windows.Devices.Radios
winrt-Windows.Foundation
winrt-Windows.Foundation.Collections
python-dotenv
websockets
pywebview
spotipy
requests
playwright
beautifulsoup4
google-auth
google-auth-oauthlib
google-api-python-client
vosk
pyttsx3
# VAD (detecta si un audio ya capturado suena a voz humana o es solo ruido).
# El paquete original "webrtcvad" no trae binarios para Windows y necesita
# Visual C++ Build Tools para compilar desde cero — este fork trae wheels
# precompilados, mismo import ("import webrtcvad"), mismo uso.
webrtcvad-wheels
# modo estudio (RAG): sentence-transformers necesita torch. Instala primero la
# versión CPU (no hace falta GPU para esto, es liviano):
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
sentence-transformers
pypdf
python-docx
fpdf2
numpy
# Detector de doble aplauso (clap_launcher.py) — mide volumen del
# micrófono en crudo, sin reconocimiento de voz.
sounddevice
# Gemini (Google): segundo respaldo en la nube si Groq falla o se queda sin
# cupo, antes de caer al modelo local. Capa gratuita en https://aistudio.google.com/apikey
google-genai
# Certificado autofirmado para el micrófono remoto del celular (HTTPS es
# obligatorio: sin eso el navegador ni deja pedir permiso de micrófono).
cryptography
